import React from "react"
import styles from './Routine_Activities.module.css'

//map and display a card for every routine_activity aleady built in the routine_activities table
//if the routine_activity belongs to the user, add the ability to edit or delete it - or add a link to their profile page to do that instead
//create a form that pulls out all routines that have activites, and build inputs to add "count" and "duration" to create a new routine_activity


const RoutineActivities= ({ token }) => {
    return (
        <div className={styles.MainDiv}>
        
        </div>
    )
}

export default RoutineActivities